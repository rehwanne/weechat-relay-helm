apiVersion: v1
kind: Service
metadata:
  name: {{ include "weechat.fullname" . }}
  labels:
    {{- include "weechat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "weechat.selectorLabels" . | nindent 4 }}
